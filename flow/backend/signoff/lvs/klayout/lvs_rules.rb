# SilicaFlow — KLayout LVS Rules (placeholder)
# Replace with process-specific LVS deck for real designs.

source($input)
schematic($schematic)
report("LVS Report", $report)

# Placeholder: basic connectivity extraction
# Real LVS requires process-specific layer definitions and device recognition
