# Synopsys Adapter Slot

Populate this directory with locally validated wrappers for tools such as Design Compiler, PrimeTime, Formality, Fusion Compiler, or ICC2.

Keep those wrappers isolated from the public-syntax reference flow so the repository contract stays stable across tool vendors.

