# Siemens Adapter Slot

Populate this directory with locally validated wrappers for tools such as Questa, Questa CDC, Tessent, or Calibre.

Keep those wrappers isolated from the public-syntax reference flow so the repository contract stays stable across tool vendors.

